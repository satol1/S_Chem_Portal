# Постпроверка (Proofreading & Style QA): ОХ-06 «Окислительно-восстановительные реакции»

- **Тема**: ОХ-06, slug `redoxReactions`, id `gen-ovr-basics`, блок `general-chemistry`
- **Дата**: 2026-08-07
- **Агент**: topic-proofreader (мандат `.qwen/agents/topic-proofreader.md`)
- **Входные данные**: тема после реализации; верификация VERIFIED 2026-08-07 (0 critical) — `reports/redox-reactions-verification-report.md`; паспорт `docs/topic-passports/redox-reactions.md`
- **Статус**: ✅ **ГОТОВО** — 0 critical / 0 minor (повторный прогон 2026-08-07 закрыл все замечания первого прогона; см. раздел «ПОВТОРНЫЙ ПРОГОН» ниже)

## Объект аудита

- `src/components/study/topics/redoxReactions/` (8 файлов, прочитаны полностью)
- ре-экспорт `src/components/study/topics/RedoxReactionsTheoryView.tsx`
- практикум: `src/data/redoxTestTasks.ts` (279 строк, прочитан полностью), `src/components/trainers/RedoxReactionsTestTrainer.tsx`
- интеграции фрагментом: `studyBlocksData.ts` (стр. 44–489, 318–423), `trainersRegistry.ts`, `StudyTopicTheoryPage.tsx`, `StudyTopicPracticePage.tsx`, `molecules.ts`, `MoleculeViewer3D.tsx` (DEFAULT_HOME_MOLECULES)

## Таблица замечаний

| № | Файл:строка | Категория | Уровень | Суть замечания | Требуемое исправление | Статус |
|---|---|---|---|---|---|---|
| 1 | `redoxReactions/RedoxReactionsInfographics.tsx:151` | язык графики | **critical** | Английское слово в SVG-подписи: узел электролизёра `title: 'Анионы Acid⁻, OH⁻'` рендерится в InfographicFigure как текст графики. Нарушение 20-RENDERING §2.5 («100% русский язык… подписи — строго на русском»; «запрет англицизмов на графике»). Единственный латинский токен-слово во всей графике темы (остальная латиница — химические символы). | Заменить на русскую формулировку: `Анионы кислотных остатков, OH⁻` либо школьное сокращение `Кисл⁻, OH⁻` | ✅ закрыто повторным прогоном |
| 2 | `redoxReactions/RedoxReactionsSections.tsx:491` | рендеринг | minor | Символ физической величины `E°` в тексте абзаца набран юникодом вне KaTeX (`...характеризует <TermTooltip .../> E° — ЭДС полуэлемента...`). 20-RENDERING §1.3 прямо относит стандартные потенциалы E° к величинам, рендерящимся через проп `math`; прецедент проекта — `halogens/HalogensSections.tsx:714` (`<ChemFormula math="E^\circ" />`). | Заменить `E°` в прозе на `<ChemFormula math="E^\circ" />`. Табличные значения E° в `font-mono` (6.5) и строки SVG-данных (`E°, В` в заголовке инфографики) не затрагивать: §1.3 допускает значения в таблицах `font-mono`, в SVG-текст KaTeX не встраивается | ✅ закрыто повторным прогоном |
| 3 | `redoxReactions/RedoxReactionsDarkBlocks.tsx:84`; `redoxReactions/RedoxReactionsInfographics.tsx:177` | рендеринг | minor | Кириллическое условие «эл. ток» внутри `\xrightarrow{...}` в math-режиме без обёртки `\text{}` (`\xrightarrow{Na_3AlF_6,\;эл.\,ток}`, `\xrightarrow{эл.\,ток}`). Рендерится без katex-error (KaTeX strict=warn, подтверждено аудитом: 0 ошибок), но стилистически расходится с правилом 3 20-RENDERING §1.2 (кириллический текст над стрелкой — в `\text{...}`), как сделано в соседних уравнениях секций 6.6/6.7 (`\xrightarrow{\text{электролиз}}`). | Обернуть кириллицу: `\xrightarrow{\text{Na}_3\text{AlF}_6,\ \text{эл. ток}}` / `\xrightarrow{\text{эл. ток}}` (или `\text{криолит, эл. ток}`) | ✅ закрыто повторным прогоном |

## Audit trail

### Раздел 1. Орфография и язык

| Проверка | Команда / паттерн | Результат |
|---|---|---|
| 1.1 Строчные названия элементов | `grep -E "[а-яё][а-яё ,.\-:;()«»]*(Углерод\|Кремний\|Азот\|Фосфор\|Кислород\|Сера\|Водород\|Хлор\|Фтор\|Бром\|Иод\|Натрий\|Железо\|Медь\|Цинк\|Алюминий\|Кальций\|Магний\|Барий\|Свинец\|Ртуть\|Серебро\|Хром\|Марганец\|Калий\|Литий)"` по папке темы | 76 совпадений, каждое просмотрено вручную: все заглавные написания — первые слова предложений/заголовков («Кислород в H₂O₂ имеет…», «Фтор — сильнейший окислитель…», «Натрий — активный металл…», «Алюминий, хлор и щёлочи…» — подзаголовок секции); в середине предложений все названия элементов строчные ✅ |
| 1.2 100% русский язык графики | полный просмотр SVG-данных (specItems, atoms/notes, легенды, chips, узлы InfographicFigure/ConceptFlow) + `grep "(title\|label\|text\|value):\s*'[^']*[A-Za-z]{3,}"` | 1 английское слово: `Acid⁻` (Infographics:151) — **critical №1**; прочая латиница — только химические символы (Li, K, MnO₄⁻, Me, e⁻) ✅/❌ |
| 1.3 Запрет номеров заданий | `grep -iE "задани[яе]\s*(№\s*)?\d\|№\s*(29\|30\|31\|32\|33)\|ЕГЭ\|ОГЭ\|ФИПИ"` по папке темы | 0 совпадений ✅ (в данных практикума «Задание N» — внутренние номера теста, допустимы) |
| 1.4 Единообразие терминологии | ручной просмотр | российская академическая школа последовательно: «степень окисления», «окислитель/восстановитель», «метод электронного баланса», «метод полуреакций (ионно-электронный метод)», «электрохимический ряд активности», «стандартный электродный потенциал», «диспропорционирование/конпропорционирование», «катод/анод» — без смешения с переводной ✅ |

### Раздел 2. Научный рендеринг (KaTeX)

| Проверка | Команда / паттерн | Результат |
|---|---|---|
| 2.1 Сырые формулы в JSX | `grep -E ">[^<>{}]*[A-Z][a-z]?[0-9][^<>]*<"` по папке темы | 0 совпадений ✅. Юникод-псевдоформулы (Fe⁺², H₂, SO₄²⁻) в прозе/данных — принятая в проекте конвенция (подтверждено по верифицированным эталонам: `reactionClassification` FunFacts/Sections, `halogens` Sections) ✅. Формулы в прозе — через `ChemText` (Sections:240, 248) ✅ |
| 2.2 Уравнения через ChemFormula; стрелки | `node scripts/audit-formulas.mjs` (прогоняет все `formula=`/`math=`/`formulae:` через реальный парсер + KaTeX) | **KATEX/PARSER ERRORS (0)** ✅ — policy-проверки неподдерживаемых стрелок и mhchem чистые; 1030 уникальных строк, 841 clean |
| 2.3 Константы через math | ручной просмотр | законы Фарадея `m = MIt/nF, F ≈ 96 485 Кл/моль` — math ✅; водородный электрод `E° ≡ 0 В` — math ✅; ряд окислительной способности галогенов — math ✅; замечание по символу E° в прозе — **minor №2** |
| 2.4 katex-error в рендере | аудит (throwOnError: true на все 1030 строк темы и studyBlocksData) + grep `katex-error` в теме | 0 ошибок; класс `katex-error` в коде темы не формируется ✅ |
| 2.5 Кириллица через проп formula | `grep -E "\-\([а-яё]\|<=\(\s*[а-яё]\|<-\([а-яё]"` по папке темы | 0 совпадений ✅; все уравнения с «электролиз» над стрелкой — через проп `math` с `\text{...}` (6.6, 6.7) ✅ |
| 2.6 SUSPICIOUS по файлам темы | секция `SUSPICIOUS ^{digit...}` вывода аудита | 20 вхождений темы, просмотрены построчно: `Mn(2+)`→`\mathrm{Mn}^{2+}`, `S(-2)`→`\mathrm{S}^{-2}`, `P(+5)`→`\mathrm{P}^{+5}`, `MnO4(2-)`→`MnO_4^{2-}`, `H2S(-2)`→`\mathrm{H_2S}^{-2}` — степени окисления и заряды в верхнем индексе, ожидаемое поведение парсера ✅ |
| mhchem / сырой LaTeX | `grep "\\ce{\|\$\\(rightarrow\|rightleftharpoons\|xrightarrow)"` | 0 совпадений ✅ |

### Раздел 3. Единообразие стиля

| Проверка | Результат |
|---|---|
| 3.1 Шрифты | корневые контейнеры TheoryView/Header/Sections: `font-body text-slate-800 leading-relaxed text-sm sm:text-base` ✅; `font-serif`/`object-cover` — grep 0 ✅; числовые данные (E° в таблице, чипы с.о.) — `font-mono` ✅ |
| 3.2 Палитра | `grep (bg\|text\|border\|…)-(red\|orange\|yellow\|green\|blue\|pink\|gray\|…)-\d` — 0 ✅ (только slate/amber/rose/emerald/teal/indigo/purple); цвета узлов инфографик/ConceptFlow — централизованные токены `getInfoCategoryColor`/категории компонентов ✅; 2D-схемы — токены `getThemePalette('dark'/'light')` ✅ |
| 3.3 Каркас секций | 9 секций; у каждой `SectionBadge` + h2 + `ScrollToNavButton`; `id="section-*"` + `scroll-mt-6` ✅; `navItems` (TheoryView) = id секций; `targetId` quick-nav (concepts/equations/electrolysis) ⊆ navItems ✅ |
| 3.4 Компоненты-дубликаты | факты — `TheoryCallout` (5 шт., FunFacts) ✅; dark-блоки — `DarkBlockCard` (4 шт.) ✅; шапка — 4 блока (карточка, «Ключевая идея», «Подводные камни», `TopicNavGrid`) ✅; модалка 2D — статический условный рендер без framer-motion ✅; quick-nav — ровно 3 тега (`Atom`/`TestTube`/`Orbit`) ✅ |
| 3.5 Бейдж экзамена / номера заданий в шапке | отсутствуют (в шапке только код ОХ-06 • Общая химия • Раздел 6) ✅ |
| 3.6 2D-превью | угловые метки с названиями («Пероксид водорода H₂O₂», «Перманганат-ион MnO₄⁻», «Дихромат-ион Cr₂O₇²⁻`»); `compact={!isModal}` — микроподписи (notes) в превью скрываются централизованно ✅ |
| 3.7 Объём теории | 9 полных секций: таблицы (3), инфографики (4), ConceptFlow, 2D-плитки (3) + светлая панель, dark-блоки (4), факты (5) — не скуднее эталона ✅ |
| 3.8 Наполнение секций | каждая секция начинается вводным абзацем теории ✅; интересные факты: 5 TheoryCallout распределены по 6.1/6.2/6.3/6.6/6.8 (по одному, не подряд) ✅; dark-блоков 4 (2 больших: 6.4 полуреакции, 6.8 Холл–Эру; 2 малых: 6.5 Бекетов, 6.8 гальваника), малые в `grid md:grid-cols-2` со светлыми партнёрами (водородный электрод; протекторная защита) ✅; содержание малых блоков не только промышленное (историческое/бытовое) ✅ |
| 3.9 Схемы и панели | светлая панель для 3 структурных формул кислот хлора (6.3) ✅; tree-диаграммы: выбор электродных процессов (6.7), продукты KMnO₄ по средам (6.2) ✅; `grep "Схема \d"` — 0 ✅; `font-serif` в схемах — 0 ✅ |

### Раздел 4. Код и интеграция

| Проверка | Команда | Результат |
|---|---|---|
| 4.1 Сборка | `npm run build` (tsc -b && vite build) | ✅ exit 0, `RedoxReactionsTheoryView-BuLNShUY.js` в бандле (128.37 kB) |
| 4.1 Линтер | `npm run lint` (oxlint) | ✅ 0 errors, 13 warnings — все вне файлов темы (MoleculeViewer2D/3D, ChemFormula, router, DifficultyBadge, UserProgressContext), существовали до темы |
| 4.2 Мёртвый код | `grep "^export"` по папке темы: 20 экспортов | все импортируются: TheoryView — ре-экспортом и lazy в StudyTopicTheoryPage; Sections/Header — в TheoryView; 4 инфографики, 4 dark-блока, 5 фактов, ConceptFlow, 2D-рендер + светлая панель — в Sections ✅ |
| 4.3 Регистрация | grep `gen-ovr-basics` | `studyBlocksData.ts:327` — внутри блока `general-chemistry` (стр. 44–489), код ОХ-06, `trainerIds: ['ovr-29','oxr-test-01','reactions-np','inorg-31-np']`, theoryLesson 6.1–6.8 ✅; все 4 trainerId — в `trainersRegistry.ts` (стр. 14, 25, 36, 80) ✅; lazy-импорт + условие `topic.id === 'gen-ovr-basics'` до generic-рендера (`StudyTopicTheoryPage.tsx:20, 91-92`) ✅; практикум: `case 'oxr-test-01'` → `RedoxReactionsTestTrainer` (`StudyTopicPracticePage.tsx:18, 69-70`) ✅; `openStudyBlock('general-chemistry','gen-ovr-basics','practice')` — blockId существует ✅; `elementStudyService.ts` — N/A (тема ОХ, маппинг ХЭ не требуется) |
| 4.4 Изображения | `grep "<img\|src=\"/\|url("` по папке темы | 0 — изображений нет, вся графика векторная/данные ✅ |
| 4.5 3D-модели | grep id в `molecules.ts`; `DEFAULT_HOME_MOLECULES` | все 9 id темы зарегистрированы: h2o(5), hno3(355), o2(576), h2o2(610), h2so4(686), cl2(757), mno4(997), nacl(1121), h2(1187) ✅; `DEFAULT_HOME_MOLECULES` (`MoleculeViewer3D.tsx:19`) = h2o/benzene/ch4/caffeine/ethanol/dopamine/gaba/glucose — молекулы темы НЕ добавлены ✅ |
| 4.6 Сверка с паспортом | чтение паспорта + кода | id секций 6.1–6.9 — 1:1 ✅; quick-nav тройка (concepts/Atom, equations/TestTube, electrolysis/Orbit) — 1:1 ✅; список 3D-молекул (9 id) — совпадает как множество ✅; отклонение «каркас 4 → 9 секций, id каркаса сохранены» заявлено в паспорте, в коде id каркаса присутствуют ✅; практикум: 12 заданий, 4 подтемы — соответствует заявленному «~12» ✅ |

### Дополнительно: практикум

- `redoxTestTasks.ts`: 12 заданий, все рендерятся централизованным `MultipleChoiceMatchingAdapter` через `ChemText` (проверено по адаптеру) ✅; mhchem/кириллица-над-стрелкой-через-formula/неподдерживаемые стрелки — grep 0 ✅; данные верифицированы (ссылка на отчёт в комментарии файла) ✅.
- `RedoxReactionsTestTrainer.tsx`: тонкая обёртка над `GenericTrainerContainer` + адаптер, без дублирования UI ✅.

## Вывод (первый прогон, 2026-08-07)

Тема не готова к коммиту: 1 critical (английское слово `Acid⁻` в SVG-подписи инфографики электролизёра, 20-RENDERING §2.5) и 2 minor (E° в прозе вне KaTeX; кириллица в `\xrightarrow` без `\text{}` в двух math-строках). После исправления — повторная постпроверка в объёме изменённых строк (достаточно перепроверить пп. 1.2, 2.3, 2.5 и перезапустить `node scripts/audit-formulas.mjs`).

---

## ПОВТОРНЫЙ ПРОГОН (2026-08-07)

- **Основание**: разработчик внёс исправления по всем трём замечаниям первого прогона. Объём повторной проверки — по вердикту первого прогона: изменённые строки (пп. 1.2, 2.3, 2.5) + полный гейт (сборка, линтер, аудит формул).

### Проверка исправлений

| Замечание | Команда / паттерн | Результат |
|---|---|---|
| critical №1 — английский токен в узле электролизёра | `grep "Acid"` по папке темы | 17 совпадений — **все в идентификаторах кода** (`id: 'acid'`, `RedoxChlorineOxyacidsLightPanel`, `OxyacidCardMeta`, `CHLORINE_OXYACIDS`, переменная `acid`, импорт в Sections:11), ни одного в UI-тексте. Узел теперь: `Infographics.tsx:151` `title: 'Анионы кислотных остатков, OH⁻'` (подтверждено `grep "кислотных остатков"` — 1 совпадение, строка 151). Полный просмотр данных `ELECTROLYZER_SPEC` (строки 134–165): все `title`/`lines`/`label` на русском. ✅ **закрыто** |
| minor №2 — E° в прозе вне KaTeX | `grep "E°"` + `grep "E\^\\circ"` по папке темы; чтение `Sections.tsx:485–534` | Обёрнуто в `<ChemFormula math="E^\circ" />`: вводный абзац 6.5 (`Sections:491`), оба заголовка таблицы (`Sections:504, 506`), сноска под таблицей (`Sections:523`) — по прецеденту `halogens/HalogensSections.tsx:714`. Оставшиеся 4 вхождения `E°` не рендерятся как нарушение: `Sections:498` — JSX-комментарий; `Infographics:109` — заголовок инфографики (явно «не затрагивать» в вердикте первого прогона: в SVG-текст KaTeX не встраивается); `Infographics:122, 127` — строковые данные справочной панели/подписи инфографики — соответствуют вердикту первого прогона и конвенции соседних верифицированных тем (`chromiumManganese` Sections/2DRenders, `sulfurOxygen` 2DRenders: `E°` юникодом в строках данных допустим); значения таблицы остались в `font-mono` без изменений. Вхождения `E°` в `redoxTestTasks.ts` (163, 173, 188) — строки данных практикума вне области §1.3 для теории, первым прогоном не отмечались. ✅ **закрыто** |
| minor №3 — кириллица в `\xrightarrow` без `\text{}` | `grep "эл\.\\,ток\|эл\.\s*ток"` по всему `src/` | **0 совпадений** во всём проекте. `grep "xrightarrow"` по папке темы — 9 вхождений, все с обёрткой `\text{...}`: `DarkBlocks:84` `\xrightarrow{\text{криолит, электролиз}}`, `Infographics:177` `\xrightarrow{\text{электролиз}}`, `Sections:606/614/630/719/724/729` `\text{электролиз}`, `Sections:622` `\text{криолит, электролиз}`. ✅ **закрыто** |

### Гейт повторного прогона

| Проверка | Команда | Результат |
|---|---|---|
| Сборка | `npm run build` (`tsc -b && vite build`) | ✅ exit 0; чанк темы в бандле — `RedoxReactionsTheoryView-CzdbAldQ.js` (128.64 kB; хэш изменился после правок — ожидаемо) |
| Линтер | `npm run lint` (oxlint) | ✅ **0 errors**; 13 warnings — тот же набор вне файлов темы (router, MoleculeViewer2D/3D, ChemFormula, DifficultyBadge, UserProgressContext), существовал до темы |
| Аудит формул | `node scripts/audit-formulas.mjs` | ✅ `TOTAL unique strings: 1030, clean: 841`; секция **`KATEX/PARSER ERRORS (0)`** пуста; изменённые строки `\xrightarrow{\text{криолит, электролиз}}` / `\xrightarrow{\text{электролиз}}` прошли реальный парсер + KaTeX без ошибок |
| SUSPICIOUS по файлам темы | секция `SUSPICIOUS ^{digit...}` вывода аудита | 20 вхождений темы — тот же набор, что в первом прогоне (ConceptFlow 52/59, DarkBlocks 37–39/63/70, Header 103/115×2, Infographics 54, Sections 110/221/224/381/442/620–622/806/826); просмотрены построчно: степени окисления и заряды в верхнем индексе — ожидаемое поведение парсера; новых нет ✅ |

### Итог повторного прогона

Все три замечания первого прогона закрыты и подтверждены grep-проверками и полным гейтом (сборка 0 ошибок, линтер 0 ошибок, KATEX/PARSER ERRORS = 0). Новых замечаний не выявлено. **Тема ОХ-06 готова к коммиту: 0 critical / 0 minor.**
