# Отчёт агента постпроверки — тема ОХ-01 «Основные понятия и законы химии» (переработка инфографики)

**Дата**: 2026-08-06 (второй цикл постпроверки темы; отчёт первого цикла — после первичной реализации — см. историю git).
**Регламент**: `.qwen/agents/topic-proofreader.md`; нормы читались с диска (`20-RENDERING.md`, `30-DESIGN.md`, `10-ARCHITECTURE.md`).
**Объект**: `src/components/study/topics/generalBasics/` + новые централизованные компоненты `src/components/scientific/svg/InfographicFigure.tsx`, `src/components/interactive/ConceptFlow.tsx`, утиль `src/utils/measureSvgText.ts`, дополнения `src/utils/molecule2DTheme.ts` (`InfoCategory`, `getInfoCategoryColor`).

**Контекст**: ошибочно использовавшийся механизм 2D-структурных рендеров (тёмные плитки + модалка) для концептуальных схем заменён по требованию владельца проекта двумя централизованными компонентами: `InfographicFigure` (статические светлые фигуры: дерево классификации, бар-чарт, мини-графики изопроцессов; авторазмеры блоков по канвас-измерению текста; справочная панель справа; подпись-вывод; без модалок) и `ConceptFlow` (React Flow: интерактивный «моль-хаб», автораскладка «хаб + спутники», двунаправленные стрелки, пунктирные дуговые связи, клик по узлу раскрывает формулу и пример). Контроль наложений подписей связей — автоматический (смещение над линией при нехватке зазора).

## Вердикт: PASS WITH MINOR — 0 critical / 4 minor (все закрыты координатором 2026-08-06)

| № | Файл | Суть | Статус |
|---|---|---|---|
| 1 | `docs/topic-passports/general-basics.md` | Паспорт рассинхронизирован: указан удалённый `GeneralBasics2DRenders.tsx` и устаревший урок про `SvgDiagramWrapper` | закрыт: паспорт обновлён в том же цикле |
| 2 | `GeneralBasicsSections.tsx` + `GeneralBasicsInfographics.tsx` | Почти дословный дубль вводного абзаца секции 1.6 и caption фигуры | закрыт: caption перефразирован как краткий вывод |
| 3 | `InfographicFigure.tsx` | Неиспользуемый ре-экспорт `export type { InfoCategory }` (второй путь импорта типа) | закрыт: ре-экспорт удалён |
| 4 | `ConceptFlow.tsx` / `GeneralBasicsConceptFlow.tsx` | Клик по хаб-узлу «моль» раскрывал заглушку (нет `details.mole`) | закрыт: добавлен `details.mole` (определение моля + сводная формула) |

## Audit trail

1. **Орфография и язык**: заглавные названия элементов в середине предложений — 0 дефектов (47 совпадений паттерна просмотрено: первые слова предложений либо ложные срабатывания); 100 % русский язык графики (латиница — только символы величин и формулы); номера заданий ЕГЭ/ОГЭ в теории — 0; российская школьная терминология выдержана.
2. **Научный рендеринг**: сырых формул в HTML-JSX — 0; уравнения через `ChemFormula`, константы через `math`; `\ce{}` — 0; кириллица в `formula` — 0; юникод-индексы в reference-панелях — по прецеденту эталонных тем; аудит формул (координатор): KATEX/PARSER ERRORS 0, SUSPICIOUS легитимны (степени 10, массовые числа; `pV = const (T)` — SVG-текст панелей, не KaTeX).
3. **Стиль**: `font-serif`/`object-cover` — 0; hex-цвета — только токены палитр (+ альфа к токену); каркасы секций, шапка, quick-nav (3 тега), иконки канонического набора (`Layers` — «схемы-диаграммы»); модалки и плитки удалены без остатка (grep `setModalDiagram|modalDiagram|ZoomIn` — 0); нумерация схем — 0; все 4 фигуры имеют заголовок и подпись-вывод, справочные панели — у всех.
4. **Код и интеграция**: сборка и линт чистые (координатор); мёртвого кода нет (`GeneralBasics2DRenders` — 0 упоминаний в `src/`); типизация без `any`; данные — типизированные константы; `only-export-components` новыми файлами не нарушен; регистрация темы (blockId, trainerIds, lazy-импорт) сверена; 3D-модели не попали в `DEFAULT_HOME_MOLECULES`.
5. **Факты**: пересчитаны и сверены с паспортом/верификацией: массы O на 14 г N (8/16/24/32/40 → 1:2:3:4:5), состав воздуха, Nₐ (точно 6.02214076·10²³, СИ 2019), Vm = 22.4 л/моль, R = 8.314 Дж/(моль·К), авторы/даты законов, примеры расчётов.
6. **Контроль наложений подписей (ConceptFlow)**: пересчёт геометрии по спеку: `mole→molar` («M = Mr, г/моль», ~93 px) > зазор ~72 px → `labelY = −34` (смещение над линией) — срабатывает верно; `mole→particles` остаётся на линии с фоновой плашкой; вертикальные рёбра пропускаются условием `|ux| ≥ |uy|`. Смещённая подпись в зоны узлов/дуг не попадает.

**Итог**: критических дефектов нет; переработка чистая (модально-плиточный механизм удалён полностью, новые компоненты централизованы, типизированы, палитрно консистентны). Тема допущена к коммиту после закрытия minor 1–4.
