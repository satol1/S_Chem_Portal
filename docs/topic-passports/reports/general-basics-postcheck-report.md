# Отчёт агента постпроверки (Proofreading & Style QA)

**Тема:** ОХ-01 «Основные понятия и законы химии» (`gen-basics-laws`, папка `src/components/study/topics/generalBasics/`)
**Дата:** 2026-08-06 · **Агент:** topic-proofreader · **Основание:** верифицированный черновик + отчёт верификации; паспорт `docs/topic-passports/general-basics.md`

## Таблица замечаний

| № | Файл:строка | Категория | Уровень | Суть замечания | Требуемое исправление |
|---|---|---|---|---|---|
| 1 | `GeneralBasics2DRenders.tsx`:133, 204, 274, 352 | стиль | minor | Формальные совпадения grep `Схема \d`: комментарии `/** Схема 1…4: … */`. В UI нумерации нет (заголовки схем — названия процессов), совпадения только в коде | Убрать номера из комментариев («Классификация веществ и смесей» и т. п.) либо мотивированно отклонить: текст не рендерится, норма 20-RENDERING §2.6 направлена против нумерации в UI |
| 2 | `GeneralBasicsSections.tsx`:779–781 (ячейки «Условия»: `T = const`, `p = const`, `V = const`), :531 (`(M ≈ 29 г/моль)`, `(M = 2 г/моль)`), :830 (`M(экв) = 98 г/моль`) | рендеринг | minor | Символьные формульные выражения набраны обычным текстом вне `ChemFormula`/`math` (20-RENDERING §1.3: формульные выражения — только KaTeX). Риск рендера отсутствует, вид корректный | Оформить через `<ChemFormula math="T = \text{const}" />` и аналоги, либо мотивированно отклонить как компактную табличную/поясняющую типографику без искажения рендера |
| 3 | `GeneralBasicsSections.tsx`:605–626 (ветви tree-диаграммы «Классификация веществ») | рендеринг | minor | Примеры формул в ветвях (`Fe, Cu, Al, Na`, `CaO, CO₂`, `HCl, H₂SO₄, HNO₃, H₃PO₄`, `(CuOH)₂CO₃` и др.) — юникод-текстом, не через `ChemFormula` (20-RENDERING §2.6: «все формулы через ChemFormula»). Подстрочные индексы юникодом отрисованы корректно, это списки примеров классов, а не уравнения | Перевести формулы ветвей в `ChemFormula` либо мотивированно отклонить: индексы визуально корректны, ветви содержат классификационные перечни, а не реакции |
| 4 | `GeneralBasicsFunFacts.tsx`:17 | орфография | minor | Лишний пробел внутри чипа: `<code …> χρῶμα</code>` — пробел попадает внутрь подсвеченного `<code>` | Убрать пробел: `<code …>χρῶμα</code>` |

**Замечаний уровня critical: 0.**

## Исправления разработчика (2026-08-06)

Все четыре замечания minor исправлены в том же цикле:
1. Номера «Схема 1…4» убраны из комментариев `GeneralBasics2DRenders.tsx` (заменены на описательные заголовки).
2. Условия изопроцессов в таблице газовых законов и значения `M ≈ 29 г/моль`, `M = 2 г/моль`, `M(экв) = 98/49 г/моль` переведены в `<ChemFormula math=… />`.
3. Все формулы в ветвях tree-диаграммы классификации переведены на `<ChemFormula formula=… />` (рендер проверен аудитом: `Al(OH)_{3}`, `Zn(OH)_{2}`, `(CuOH)_{2}CO_{3}` — индексы внизу).
4. Пробел внутри `<code>` убран.

Контроль после исправлений: `npm run build` — exit 0; `node scripts/audit-formulas.mjs` — KATEX/PARSER ERRORS: 0.

## Audit trail

### 1. Орфография и язык

| Пункт | Проверка (паттерн/команда) | Результат |
|---|---|---|
| 1.1 Строчные названия элементов | grep `[а-яё][а-яё ,.\-]*(Углерод\|Кремний\|Азот\|…\|Марганец)` по папке темы — 49 совпадений + контрольный регистронезависимый grep тех же имён — 60 совпадений; каждое просмотрено вручную | ✅ Заглавных названий элементов внутри предложений нет. Единственное заглавное «Кислород» (`Sections.tsx:427`) — первое слово предложения (допустимо). Ложные срабатывания подстрок: «Хроматография» («Хром»), «Периодического» («иод»), «Железные опилки» (первое слово ячейки, прилагательное). В практикуме (`generalBasicsTestTasks.ts`) — 4 совпадения, все строчные |
| 1.2 100% русский язык графики | grep `[a-zA-Z]{3,}` по `GeneralBasics2DRenders.tsx` (282 совпадения — просмотрены: подавляющее большинство идентификаторы кода/JSX-атрибуты); чтение всех `title`/`specItems`/SVG-`<text>` файла | ✅ Все заголовки, легенды, подписи ветвей и `specItems` — русские. Латиница только в международных обозначениях величин и математической нотации (`pV = const (T)`, оси `V/p/T`, `n = m/M`, `Nₐ`) — не английские надписи |
| 1.3 Запрет номеров заданий | grep `задани[яе]\s*(№\s*)?\d\|№\s*(8\|9\|29\|30\|31\|32\|33)\|ЕГЭ\|ОГЭ\|ФИПИ` по папке темы | ✅ 0 совпадений |
| 1.4 Единообразие терминологии | Чтение всех 6 файлов темы | ✅ Российская академическая школа: «далтониды/бертоллиды», «н. у.», «фактор эквивалентности», «молярный объём», датировки по российской традиции (Пруст 1801, Рихтер 1793) с оговорками |

### 2. Научный рендеринг (KaTeX)

| Пункт | Проверка | Результат |
|---|---|---|
| 2.1 Сырые формулы в JSX | grep `>[^<>]*[A-Z][a-z]?[0-9][^<>]*<` по папке темы | ✅ 0 совпадений. Юникод-формулы в прозе (H₂O, FeₓO, ³⁵Cl в таблице `font-mono`) индексы отрисовывают корректно, «сырыми» ASCII-формулами не являются; табличные нуклиды — данные `font-mono` (допустимо §1.3) |
| 2.2 Уравнения и стрелки | grep `<==>\|<-->\|<=\|<-` по папке темы; просмотр всех `->` | ✅ 0 совпадений нестандартных стрелок. Все 4 уравнения (`CH4 + 2O2 -> …`, `2H2 + O2 -> …`, `H2 + Cl2 -> …`, `H2SO4 + 2NaOH -> …`) — в пропах `formula` `ChemFormula`, без условий и разрывов стрелки. Юникод `→` в тексте (ряд оксидов, 1.6) разрешён §1.5. `<sub>/<sup>` — 0 совпадений |
| 2.3 Константы через `math` | Чтение секций | ✅ Все величины через `math`: `V_m = 22.4`, `A = Z + N`, а.е.м., `N_A`, `R = 8.314…`, `x_i`, `M_экв`-формулы и т. д. Замечание № 2 таблицы — три оставшихся текстовых выражения |
| 2.4 `katex-error` | grep `katex-error` по папке темы | ✅ 0 совпадений |
| 2.5 Кириллические условия через `formula` | grep `-\([а-яё]` по папке темы | ✅ 0 совпадений (условий над стрелкой в теме нет; исторические уравнения в dark-блоке оформлены через `math`: `2Hg + O_2 \rightarrow 2HgO`) |
| 2.6 Аудит формул | `node scripts/audit-formulas.mjs` (запущен агентом) | ✅ Exit 0. `TOTAL: 930 строк, clean: 757`. **KATEX/PARSER ERRORS: 0**. SUSPICIOUS по файлам темы — 9 позиций, просмотрены построчно: `^{12}C`/`^{35}Cl` — массовые числа (вверху верно); `10^{23}`, `10^{24}`, `\text{моль}^{-1}` — порядки величин (верно); `pV = const (T)` и ещё 2 — строковые SVG-подписи изопроцессов (проп `formula` компонента `IsoprocessPanel`, не KaTeX). Подтверждаю оценку координатора |

### 3. Единообразие стиля

| Пункт | Проверка | Результат |
|---|---|---|
| 3.1 Шрифты | Чтение корневых контейнеров; grep `font-serif\|object-cover` | ✅ Корень TheoryView/Header/Sections: `font-body text-slate-800 leading-relaxed text-sm sm:text-base`; `font-serif`/`object-cover` — 0 совпадений; данные/чипы/метки — `font-mono` (`fontFamily="sans-serif"` в SVG ≠ запрещённый `font-serif`) |
| 3.2 Палитра | grep `#[0-9a-fA-F]{3,8}` по папке темы | ✅ 0 произвольных hex: цвета схем — токены `getThemePalette('dark')`; Tailwind — только slate/amber/emerald/teal/rose |
| 3.3 Каркас секций | grep `id="section-` (12), `SectionBadge` (12 исп.), `ScrollToNavButton` (12 исп.) | ✅ Все 12 секций: `section-*` + `scroll-mt-6` + бейдж + кнопка; id = `navItems` = `targetId` quick-nav = паспорт |
| 3.4 Централизованные компоненты | Чтение импортов | ✅ Факты — `TheoryCallout`, dark-блоки — `DarkBlockCard`, шапка — 4 блока, модалка статическая (классы 1:1 с 20-RENDERING §2.4), quick-nav — ровно 3 тега (`Atom`/`TestTube`/`Orbit`). Локальных дублей нет (`TermChips`, `SchemeBox`, `Branch`, `IsoprocessPanel` — внутренние презентационные подкомпоненты файла, не UI-дубли) |
| 3.5 Шапка без бейджа экзамена/заданий | Чтение `GeneralBasicsHeader.tsx` | ✅ 4 подводных камня с mono-чипами, без бейджа экзамена и номеров заданий |
| 3.6 2D-превью | Чтение плиток | ✅ 4 плитки с живым SVG, оверлей `ZoomIn`, угловые метки: «Вещества и смеси» (16), «Оксиды азота» (12), «Моль и расчёты» (14), «Газовые законы» (14) — все ≤ 22 символов |
| 3.7 Объём теории | Подсчёт | ✅ (применяется по аналогии, профиль ОХ): 12 разделов, `ChemFormula` ≈ 65 употреблений, `TermTooltip` 9, 4 таблицы, 4 типа 2D-схем, tree-диаграмма, 9 3D-молекул, 3 dark-блока, 3 факта — не скуднее эталона |
| 3.8 Наполнение секций | Чтение секций | ✅ Каждая секция начинается с 1–2 вводных абзацев; 3 факта распределены по 1.1/1.4/1.9; dark-блоки: 2 больших (1.3, 1.5) + 1 малый (1.7) в `grid grid-cols-1 md:grid-cols-2` со светлой карточкой-партнёром «Следствия закона Авогадро»; содержание историко-понятийное, не только промышленное |
| 3.9 Схемы и панели | grep `Схема \d\|Рис\.\|Рисунок`; чтение схемы 1.8 | ⚠️ 4 совпадения — только комментарии (замечание № 1). Tree-диаграмма классификации в 1.8: без нумерации в UI, без `font-serif`, русская, `overflow-x-auto` + `min-w-[720px]`, ветви подписаны. Светлые панели — у всех 4 концептуальных схем (`bg-slate-50 border-slate-200`) |

### 4. Код и интеграция

| Пункт | Проверка | Результат |
|---|---|---|
| 4.1 Сборка и линт | По данным координатора от 2026-08-06 (по заданию не перезапускались) | ✅ `npm run build` — exit 0; `npm run lint` — 0 ошибок (13 предсуществующих warnings вне файлов темы) |
| 4.2 Мёртвый код | grep `export const` по папке темы (10) → сверка импортов | ✅ Все 10 экспортов импортированы (TheoryView → ре-экспорт и `StudyTopicTheoryPage`; Header/Sections → TheoryView; 3 факта + 3 dark-блока + `GeneralBasics2DRender` → Sections). `export default` тренажёра — общая конвенция всех тренажёров проекта. Неиспользуемых «просмотрщиков» нет |
| 4.3 Регистрация | grep по `studyBlocksData.ts`, `StudyTopicTheoryPage.tsx`, `trainersRegistry.ts`, `StudyTopicPracticePage.tsx` | ✅ Блок `general-chemistry` существует (строка 45); тема `gen-basics-laws` с кодом `ОХ-01`, `trainerIds: ['gb-test-01', 'np-test-14-1']` — оба id существуют в `TRAINERS_REGISTRY`; lazy-импорт (`StudyTopicTheoryPage.tsx:15`) и условие (`:81`) до generic-рендера; case `gb-test-01` в `StudyTopicPracticePage.tsx:63`; `openStudyBlock('general-chemistry', 'gen-basics-laws', 'practice')` — blockId реальный. Маппинг `elementStudyService` не требуется (профиль ОХ). Бейдж блока «8 Тем» соответствует 8 темам ОХ-01…ОХ-08 |
| 4.4 Изображения | grep `<img\|src=\|url\(` по папке темы | ✅ 0 растровых изображений; единственные `url(#gb-tree-arrow)` — внутренние SVG-маркеры |
| 4.5 3D-модели | grep `id: 'h2'\|id: 'n2'` в `molecules.ts`; чтение конца массива; grep `DEFAULT_HOME_MOLECULES` в `MoleculeViewer3D.tsx` | ✅ `h2` (стр. 1187) и `n2` (стр. 1203) добавлены в конце `MOLECULES_DATA`, полное соответствие интерфейсу `Molecule`; все 9 id темы (`h2, n2, o2, h2o, co2, ch4, nh3, hcl, cl2`) существуют. `DEFAULT_HOME_MOLECULES = ['h2o', 'benzene', 'ch4', 'caffeine', 'ethanol', 'dopamine', 'gaba', 'glucose']` — **`h2`/`n2` не добавлены** |
| 4.6 Консистентность с паспортом | Сверка `docs/topic-passports/general-basics.md` с кодом | ✅ 12 id секций совпадают 1:1; quick-nav тройка совпадает; 4 типа 2D-рендеров совпадают; список из 9 3D-молекул совпадает; `trainerIds` совпадают. Заявленные отклонения подтверждены фактически: 12 разделов (навигация TheoryView); тренажёра нет в `trainersCatalog.ts` (grep — 0 совпадений), доступ через `trainerIds` |
| 4.7 Практикум | Чтение `generalBasicsTestTasks.ts`, `GeneralBasicsTestTrainer.tsx` | ✅ 14 заданий (строки 21–316), подтемы суммируются в 14 (2+2+3+2+2+2+1); обёртка на `GenericTrainerContainer` + `MultipleChoiceMatchingAdapter` по паттерну N/P; язык заданий русский, названия элементов строчные |

## Вердикт

**Тема ОХ-01 готова к коммиту: 0 замечаний уровня critical.** Четыре замечания уровня minor (таблица) не блокируют коммит при исправлении либо мотивированном отклонении разработчиком — все они не влияют на рендер и научную корректность (комментарии, компактная табличная типографика, юникод-индексы в классификационных перечнях, один пробел в чипе).
